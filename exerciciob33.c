//Read the side size your square and show with result your zone.
#include <stdio.h>
    int main(){
        float l;
        printf("How is side size your square? ");
        scanf("%f",&l);
        l=l*l;
        printf("Your zone is igual %.2f",l);
    }
